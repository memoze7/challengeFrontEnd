
import { createAction } from '@ngrx/store';

export const AbrirModalAuth = createAction(
  '[Auth] Abrir Modal Auth'
);

export const OcultarModalAuth = createAction(
  '[Auth] Ocultar Modal Auth'
);
export const CerrarModalAuth = createAction(
  '[Auth] Cerrar Modal Auth'
);



