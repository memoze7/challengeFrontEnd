
import { createAction, props } from '@ngrx/store';

export const AbrirModalModalDetail = createAction(
  '[ModalDetail] Abrir Modal Modal Detail'
);

export const OcultarModalModalDetail = createAction(
  '[ModalDetail] Ocultar Modal Modal Detail'
);
export const CerrarModalModalDetail = createAction(
  '[ModalDetail] Cerrar Modal Modal Detail'
);

